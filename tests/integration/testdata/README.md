# Integration fixtures

These sample files support CLI integration scenarios for:

- Known extension-based kinds
- Extensionless magic-byte classification
- Generic `.bin` fallback behavior
- Unreadable-file scenarios created dynamically in tests
